services:
  - type: web
    name: networking-match
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: ADMIN_KEY
        sync: false
      - key: BASE_URL
        sync: false
